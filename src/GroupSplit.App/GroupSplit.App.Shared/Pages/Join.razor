@page "/join/{Token}"
@using GroupSplit.App.Shared.Components
@using GroupSplit.App.Shared.Services
@using GroupSplit.App.Shared.Services.Commands
@using GroupSplit.App.Shared.Services.Errors
@using GroupSplit.Shared
@attribute [AllowAnonymous]
@inject IInvitationsClient Invitations
@inject IGroupCommands GroupCommands
@inject ApiErrorPresenter Errors
@inject IAuthService AuthService
@inject NavigationManager Nav

@*
    Where a shared link lands.

    Anonymous, because the person following one very often has no account yet -- that is the
    case invitations exist for and the one an email to an unconfirmed address serves worst.
    Signing in is a round trip through the identity provider, so the URL they were going to
    is handed along as returnUrl and they come back here rather than to the home page, with
    the link still in hand.
*@

<PageTitle>Join a group - Group Split</PageTitle>

<div class="gs-empty" style="padding-top: 64px; max-width: 32rem; margin: 0 auto">

    @if (UserInfo is null)
    {
        <MudIcon Icon="@Icons.Material.Rounded.Groups" />
        <h1 style="font-size: 1.375rem; margin: 0">You have been invited to a group</h1>
        <span>Sign in, or create an account, and we will bring you straight back here to join.</span>

        <MudButton Variant="Variant.Filled" Color="Color.Primary" Class="gs-btn-pill mt-4"
                   EndIcon="@Icons.Material.Rounded.ArrowForward"
                   OnClick="@(() => AuthService.Login($"/join/{Token}"))">Continue</MudButton>
    }
    else if (_loading)
    {
        <MudProgressCircular Color="Color.Primary" Indeterminate />
        <span>Looking up the group…</span>
    }
    else if (_error is not null)
    {
        @* A link that has run out and one the group withdrew are different things to have
           happened, and the message says which -- that is the whole reason the API keeps
           them apart instead of answering "not found" to both. *@
        <MudIcon Icon="@Icons.Material.Rounded.LinkOff" />
        <h1 style="font-size: 1.375rem; margin: 0">This link will not let you in</h1>
        <span>@_error</span>

        <MudButton Variant="Variant.Text" Class="mt-4" Href="/"
                   StartIcon="@Icons.Material.Rounded.ArrowBack">Back home</MudButton>
    }
    else if (_link is not null)
    {
        <Avatar Name="@_link.GroupName" />
        <h1 style="font-size: 1.375rem; margin: 0">@_link.GroupName</h1>

        <span>
            @if (_link.AlreadyAMember)
            {
                @("You are already in this group.")
            }
            else
            {
                @(string.IsNullOrWhiteSpace(_link.CreatedByUserName)
                    ? $"{Members(_link.MemberCount)} already. Join to start sharing expenses with them."
                    : $"{_link.CreatedByUserName} shared this group with you. {Members(_link.MemberCount)} already.")
            }
        </span>

        <MudButton Variant="Variant.Filled" Color="Color.Primary" Class="gs-btn-pill mt-4"
                   EndIcon="@Icons.Material.Rounded.ArrowForward"
                   Disabled="@_joining"
                   OnClick="@JoinAsync">
            @(_link.AlreadyAMember ? $"Open {_link.GroupName}" : $"Join {_link.GroupName}")
        </MudButton>
    }

</div>

@code {
    [Parameter] public string Token { get; set; } = string.Empty;

    [CascadingParameter] public UserInfo? UserInfo { get; set; }

    private JoinLinkResponse? _link;
    private string? _error;
    private bool _loading;
    private bool _joining;

    /// <summary>The token the state below belongs to, so it is not re-read on every render.</summary>
    private string? _resolved;

    /// <summary>
    /// On every parameter set rather than only on initialise, because signing in is the
    /// step in the middle: the person arrives here with no account, comes back with one,
    /// and it is the cascading user changing that has to send this looking.
    /// </summary>
    protected override async Task OnParametersSetAsync()
    {
        if (UserInfo is null || _loading || _resolved == Token)
            return;

        _loading = true;
        _error = null;

        var failure = await Errors.CaptureAsync(async () =>
            _link = await Invitations.GetJoinLinkAsync(Token));

        // Shown on the page rather than in a snackbar: this is the only thing on the page,
        // and a message that scrolls away would leave nothing behind it.
        if (failure is not null)
        {
            _link = null;
            _error = failure.Message;
        }

        _resolved = Token;
        _loading = false;
    }

    private async Task JoinAsync()
    {
        if (_link is null) return;

        _joining = true;

        var joined = await GroupCommands.JoinByLinkAsync(Token);

        _joining = false;

        // Being in it already lands in the same place, because it is the same place they
        // were trying to get to.
        if (joined is not null)
            Nav.NavigateTo($"/groups/{joined.GroupId}");
    }

    private static string Members(int count) =>
        count == 1 ? "One person is in it" : $"{count} people are in it";
}
