<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\GroupSplit.Jobs\GroupSplit.Jobs.csproj" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" Version="10.0.11" />
        <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.11" />
    </ItemGroup>

    <ItemGroup>
        <!-- The queue, pump and scheduler are internal: a host chooses them through
             AddInProcessJobs() and never names them. The tests name them. -->
        <InternalsVisibleTo Include="GroupSplit.API.Test" />
    </ItemGroup>

</Project>
