@page "/transactions"
@using GroupSplit.App.Shared.Components
@using GroupSplit.App.Shared.Services.Transactions
@using GroupSplit.Shared
@inject ITransactionsService TransactionsService
@inject ISnackbar Snackbar
@inject MudTheme Theme

<PageTitle>Transactions</PageTitle>

<MudStack Row="true" AlignItems="AlignItems.Center">
    <MudStack Spacing="1">
        <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
            <MudIcon Icon="@Icons.Material.Outlined.Receipt" Color="Color.Primary" Size="Size.Large"/>
            <MudText Typo="Typo.h5" Class="font-weight-bold">Transactions</MudText>
        </MudStack>
    </MudStack>
</MudStack>


<MudTextField @bind-Value="_searchString"
              Placeholder="Search transactions..."
              Adornment="Adornment.Start"
              AdornmentIcon="@Icons.Material.Outlined.Search"
              IconSize="Size.Medium"
              Immediate="true"
              Style="@Theme.PaletteLight.Surface.BackgroundColorStyle"
              Variant="Variant.Outlined"
              Class="flex-grow-1 pa-6"/>

<MudDataGrid T="TransactionResponse"
             Items="@TransactionsService.UserTransactions"
             Filterable="true"
             FilterMode="DataGridFilterMode.Simple"
             Hover="true"
             Dense="false"
             Bordered="false"
             Class="ma-6"
             Striped="true">
    <Columns>
        <PropertyColumn Property="x => x.DateTime" Title="Date" Filterable="false">
            <CellTemplate>
                <MudStack Spacing="0">
                    <MudText Typo="Typo.body2">@context.Item.DateTime.LocalDateTime.ToShortDateString()</MudText>
                    <MudText Typo="Typo.caption">@context.Item.DateTime.LocalDateTime.ToShortTimeString()</MudText>
                </MudStack>
            </CellTemplate>
        </PropertyColumn>

        <PropertyColumn Property="x => x.Name" Title="Name" Filterable="true">
            <CellTemplate>
                <MudText Typo="Typo.body2">@context.Item.Name</MudText>
            </CellTemplate>
        </PropertyColumn>

        <PropertyColumn Property="x => x.GroupName" Title="Group" Filterable="true">
            <CellTemplate>
                <MudChip T="string" Color="Color.Primary" Size="Size.Small" Variant="Variant.Outlined">
                    @context.Item.GroupName
                </MudChip>
            </CellTemplate>
        </PropertyColumn>

        <PropertyColumn Property="x => x.Amount" Title="Amount" Filterable="false">
            <CellTemplate>
                <MudText Typo="Typo.body1" Class="font-weight-bold" Color="Color.Success">
                    $@context.Item.Amount.ToString("N2")
                </MudText>
            </CellTemplate>
        </PropertyColumn>

        <TemplateColumn Title="Actions" Filterable="false">
            <CellTemplate>
                <MudStack Row="true" Spacing="1">
                    <MudIconButton Icon="@Icons.Material.Outlined.Edit"
                                   Size="Size.Small"
                                   Color="Color.Primary"
                                   OnClick="@(async () => await EditTransactionAsync(context.Item))"/>
                    <MudIconButton Icon="@Icons.Material.Outlined.Delete"
                                   Size="Size.Small"
                                   Color="Color.Primary"
                                   OnClick="@(async () => await DeleteTransactionAsync(context.Item))"/>
                </MudStack>
            </CellTemplate>
        </TemplateColumn>
    </Columns>
    <NoRecordsContent>
        <MudStack AlignItems="AlignItems.Center" Spacing="2" Class="pa-8">
            <MudIcon Icon="@Icons.Material.Outlined.SearchOff" Size="Size.Large" Color="Color.Secondary"/>
            <MudText Typo="Typo.body1" Color="Color.Secondary">No transactions found</MudText>
        </MudStack>
    </NoRecordsContent>
</MudDataGrid>

<ExtendedFab Label="Transaction" OnClick="@AddTransactionAsync"/>

@code {
    private string _searchString = "";

    protected override async Task OnInitializedAsync()
    {
        if (TransactionsService.UserTransactions is null)
        {
            await TransactionsService.LoadAsync();
            StateHasChanged();
        }
    }

    private async Task AddTransactionAsync()
    {
        var request = await TransactionsService.ShowCreateTransactionDialogAsync();
        if (request is null) return;

        try
        {
            await TransactionsService.AddAsync(request);
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error adding transaction: {ex.Message}", Severity.Error);
        }
    }

    private async Task EditTransactionAsync(TransactionResponse transaction)
    {
        var request = await TransactionsService.ShowEditTransactionDialogAsync(transaction);
        if (request is null) return;

        try
        {
            await TransactionsService.EditAsync(transaction.Id, request);
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error editing transaction: {ex.Message}", Severity.Error);
        }
    }

    private async Task DeleteTransactionAsync(TransactionResponse transaction)
    {
        if (!await TransactionsService.ShowConfirmDeleteDialogAsync(transaction)) return;

        try
        {
            await TransactionsService.DeleteAsync(transaction);
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error deleting transaction: {ex.Message}", Severity.Error);
        }
    }

}
